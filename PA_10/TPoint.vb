Public Class TPoint 'Class for points, contains the x coordinate and y coordinate
    Public x, y As Integer
    Public Sub setCoordinates(forX As Integer, forY As Integer) 'Set all coordinates
        x = forX
        y = forY
    End Sub
End Class
